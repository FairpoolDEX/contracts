import { Contract } from 'ethers'
import { FrozenWallet } from '../FrozenWallet'
import { impl } from '../../util/todo'

export async function expectTokenFrozenWallets(token: Contract, expectedWallets: FrozenWallet[]) {
  throw impl()
}
